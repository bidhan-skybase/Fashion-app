import {ScrollView, Text, View} from "react-native";

const Home=()=>{
    return (
        <ScrollView>
            <View>
                <Text> Home</Text>
            </View>
        </ScrollView>
    );
}
export default Home;
